# 前端 Vite 环境变量（复制为 .env.development.local 等按需修改）
#
# 生产构建（npm run build）会读取 .env.production；其中 VITE_PAY_API_BASE 必须指向
# 部署了完整 server/index.js 的 HTTPS 域名（含 /api/auth、/api/pay、/api/jobs 等）。
# 若指向仅含部分路由的子域，会出现 /api/auth/oauth-config、send-phone-code 等 404。
#
# 本地开发：不要设置 VITE_PAY_API_BASE，让请求走相对路径 /api，由 vite.config.ts 代理到 127.0.0.1:8787。
# VITE_PAY_API_BASE=https://你的完整API域名
